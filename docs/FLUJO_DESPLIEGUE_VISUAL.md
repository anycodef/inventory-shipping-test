# 📊 Flujo de Despliegue Visual

##  Arquitectura General

```
┌─────────────────────────────────────────────────────────┐
│           Tu Computadora (Local)                        │
├─────────────────────────────────────────────────────────┤
│  1. Código fuente                                       │
│  2. Scripts de PowerShell                               │
│  3. Docker Desktop                                      │
└──────────────────┬──────────────────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────────────────┐
│           Google Cloud Platform                         │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  ┌─────────────────┐      ┌──────────────────┐        │
│  │ Artifact        │◄─────┤ Cloud Run        │        │
│  │ Registry        │      │ (Servicios)      │        │
│  │ (Imágenes)      │      └─────────┬────────┘        │
│  └─────────────────┘                │                  │
│                                     │                  │
│                          ┌──────────▼─────────┐        │
│                          │ Cloud SQL          │        │
│                          │ (PostgreSQL)       │        │
│                          └────────────────────┘        │
└─────────────────────────────────────────────────────────┘
```

---

## 🔄 Proceso de Despliegue Paso a Paso

### Paso 1: Preparación Local

```
📁 Tu Computadora
│
├── 📝 Código actualizado (git pull)
├── 🐳 Docker Desktop corriendo
└── ☁️ Google Cloud SDK configurado
```

**Comandos:**
```powershell
cd inventory-shipping
git pull origin master
docker ps  # Verificar Docker
gcloud config get-value project  # Verificar GCP
```

---

### Paso 2: Build de Imagen Docker

```
┌──────────────────────────┐
│   Código del Servicio    │
│   + Dependencias         │
│   + Prisma Migrations    │
└───────────┬──────────────┘
            │
            ▼
    [ docker build ]
            │
            ▼
┌──────────────────────────┐
│   Imagen Docker          │
│   (Contenedor empaquetado)│
└──────────────────────────┘
```

**Script ejecuta:**
```powershell
docker build -t us-central1-docker.pkg.dev/secure-potion-474303-j7/microservices-repo/shipping:latest .
```

**¿Qué incluye la imagen?**
- ✅ Código fuente (src/)
- ✅ package.json y dependencias
- ✅ Prisma schema y migraciones
- ✅ docker-entrypoint.sh
- ✅ Node.js runtime

---

### Paso 3: Push a Artifact Registry

```
┌──────────────────────┐
│  Imagen Docker Local │
└──────────┬───────────┘
           │
           │ docker push
           │
           ▼
┌──────────────────────────────────┐
│  Artifact Registry (GCP)         │
│  ┌────────┬────────┬────────┐   │
│  │shipping│store   │inventory│  │
│  │:latest │:latest │:latest  │  │
│  └────────┴────────┴────────┘   │
└──────────────────────────────────┘
```

**Script ejecuta:**
```powershell
docker push us-central1-docker.pkg.dev/.../shipping:latest
```

---

### Paso 4: Deploy a Cloud Run

```
┌─────────────────────────────────────────┐
│   Artifact Registry                     │
│   └─ shipping:latest                    │
└─────────────┬───────────────────────────┘
              │
              │ gcloud run deploy
              │
              ▼
┌─────────────────────────────────────────┐
│   Cloud Run                             │
│   ┌───────────────────────────────┐    │
│   │  shipping-service             │    │
│   │  - Contenedor corriendo       │    │
│   │  - URL pública                │    │
│   │  - Auto-scaling               │    │
│   └───────────────────────────────┘    │
└─────────────────────────────────────────┘
```

**Script ejecuta:**
```powershell
gcloud run deploy shipping-service \
  --image=[IMAGEN] \
  --region=us-central1 \
  --allow-unauthenticated \
  --set-env-vars="DATABASE_URL=..." \
  --add-cloudsql-instances="..."
```

---

### Paso 5: Inicialización del Contenedor

```
┌────────────────────────────────────────┐
│  Cloud Run inicia el contenedor       │
└────────────────┬───────────────────────┘
                 │
                 ▼
     [ docker-entrypoint.sh ]
                 │
    ┌────────────┼────────────┐
    │            │            │
    ▼            ▼            ▼
┌────────┐  ┌────────┐  ┌────────┐
│ Prisma │  │ Seed   │  │ Start  │
│Generate│  │Database│  │Server  │
└────────┘  └────────┘  └────────┘
    │            │            │
    └────────────┴────────────┘
                 │
                 ▼
┌────────────────────────────────────────┐
│  Servicio corriendo en Cloud Run      │
│  https://shipping-service-xxx.run.app  │
└────────────────────────────────────────┘
```

**Secuencia exacta:**
1. `npx prisma generate` - Genera cliente Prisma
2. `npx prisma db push` - Aplica migraciones a Cloud SQL
3. `node prisma/seed.js` - Datos iniciales (opcional)
4. `node src/index.js` - Inicia servidor Express

---

### Paso 6: Conexión a Cloud SQL

```
┌─────────────────────────────────┐
│  shipping-service               │
│  (Cloud Run Container)          │
└────────────┬────────────────────┘
             │
             │ Unix Socket
             │ /cloudsql/PROJECT:REGION:INSTANCE
             │
             ▼
┌─────────────────────────────────┐
│  Cloud SQL PostgreSQL           │
│  ┌────────┬────────┬─────────┐ │
│  │shipping│store   │inventory│ │
│  └────────┴────────┴─────────┘ │
└─────────────────────────────────┘
```

**Conexión configurada:**
```
postgresql://postgres:PASSWORD@localhost/shipping?host=/cloudsql/PROJECT:REGION:INSTANCE
```

---

## 📊 Timeline del Despliegue

```
0:00 ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ Inicio
     │
     ├─ Validaciones iniciales (10s)
     │
0:10 ├─ Build imagen Docker (2-3 min)
     │  └─ Descarga dependencias
     │  └─ Compila código
     │  └─ Crea layers
     │
3:00 ├─ Push a Artifact Registry (1-2 min)
     │  └─ Sube layers
     │  └─ Verifica integridad
     │
5:00 ├─ Deploy a Cloud Run (1-2 min)
     │  └─ Actualiza servicio
     │  └─ Espera healthy
     │
7:00 ├─ Inicialización Container (30s)
     │  └─ Prisma generate
     │  └─ DB push
     │  └─ Start server
     │
7:30 ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━ Completado ✅
```

**Tiempo total: 7-8 minutos** (primera vez puede ser 10-15 min)

---

## 🔄 Flujo Completo en Diagrama

```
┌─────────────────┐
│ git pull        │
└────────┬────────┘
         │
         ▼
┌─────────────────────────────────────────────┐
│ .\deploy-single-service.ps1 -ServiceName X  │
└────────┬────────────────────────────────────┘
         │
         ├──► 1. cd services/X
         │
         ├──► 2. docker build
         │        │
         │        └──► Dockerfile
         │             ├─ FROM node:18-alpine
         │             ├─ COPY package*.json
         │             ├─ RUN npm install
         │             ├─ COPY prisma/
         │             ├─ COPY src/
         │             └─ CMD ["sh", "docker-entrypoint.sh"]
         │
         ├──► 3. docker push
         │        │
         │        └──► Artifact Registry
         │             us-central1-docker.pkg.dev/
         │             secure-potion-474303-j7/
         │             microservices-repo/X:latest
         │
         ├──► 4. gcloud run deploy
         │        │
         │        └──► Cloud Run Service
         │             ├─ Región: us-central1
         │             ├─ Memory: 512Mi
         │             ├─ CPU: 1
         │             ├─ Min instances: 0
         │             ├─ Max instances: 10
         │             └─ Env vars: DATABASE_URL
         │
         └──► 5. Contenedor inicia
                  │
                  ├──► docker-entrypoint.sh
                  │    ├─ npx prisma generate
                  │    ├─ npx prisma db push ──┐
                  │    ├─ node prisma/seed.js   │
                  │    └─ node src/index.js     │
                  │                              │
                  └──► Cloud SQL ◄──────────────┘
                       postgresql://...
                       
                  ✅ Servicio en producción
                  https://X-service-xxx.run.app
```

---

## 🎯 Estados del Servicio

### Durante el Despliegue:

```
┌─────────────────────────────────────┐
│ Estado: Deploying                   │
│ ┌─────────────────────────────────┐ │
│ │ Old Version: shipping-00042     │ │
│ │ Status: Serving 100% traffic    │ │
│ └─────────────────────────────────┘ │
│                                     │
│ ┌─────────────────────────────────┐ │
│ │ New Version: shipping-00043     │ │
│ │ Status: Starting up...          │ │
│ └─────────────────────────────────┘ │
└─────────────────────────────────────┘
```

### Después del Despliegue:

```
┌─────────────────────────────────────┐
│ Estado: Ready                       │
│ ┌─────────────────────────────────┐ │
│ │ Version: shipping-00043         │ │
│ │ Status: Serving 100% traffic ✅ │ │
│ │ URL: https://shipping-service... │ │
│ └─────────────────────────────────┘ │
│                                     │
│ ┌─────────────────────────────────┐ │
│ │ Old Version: shipping-00042     │ │
│ │ Status: Standby (0% traffic)    │ │
│ │ Ready for rollback if needed    │ │
│ └─────────────────────────────────┘ │
└─────────────────────────────────────┘
```

---

## 🔍 Verificación Post-Despliegue

```
┌──────────────────────────────────────┐
│  Verificaciones Automáticas          │
├──────────────────────────────────────┤
│                                      │
│  1. Health Check                     │
│     GET /health                      │
│     ✓ Status 200                     │
│     ✓ Response: {"status":"ok"}     │
│                                      │
│  2. Database Connection              │
│     ✓ Cloud SQL accesible            │
│     ✓ Tablas creadas/actualizadas    │
│                                      │
│  3. Environment Variables            │
│     ✓ NODE_ENV=production            │
│     ✓ DATABASE_URL configurado       │
│                                      │
│  4. Container Logs                   │
│     ✓ No errores críticos            │
│     ✓ Servidor iniciado              │
│                                      │
└──────────────────────────────────────┘
```

---

## 📝 Comparación: Local vs Cloud

| Aspecto | Desarrollo Local | Producción (Cloud Run) |
|---------|------------------|------------------------|
| **Código** | Tu máquina | Contenedor en GCP |
| **Base de Datos** | PostgreSQL local | Cloud SQL |
| **URL** | localhost:4003 | https://...run.app |
| **Escalamiento** | 1 instancia | 0-10 instancias automático |
| **Costo** | Gratis | Pay-per-use |
| **Migraciones** | Manual | Automático (entrypoint.sh) |
| **Logs** | Terminal local | Cloud Logging |
| **Backup BD** | Manual | Automático diario |

---

## 🚀 Resumen del Flujo

```
Developer → git push → Código actualizado
                │
                ▼
         Script PowerShell
                │
    ┌───────────┼───────────┐
    │           │           │
    ▼           ▼           ▼
  Build       Push       Deploy
  Docker   Artifact    Cloud Run
  Local     Registry      │
                          │
                    ┌─────┴─────┐
                    │           │
                    ▼           ▼
              Start        Connect
           Container      Cloud SQL
                │           │
                └─────┬─────┘
                      │
                      ▼
              ✅ Servicio Live
          https://service.run.app
```

---

**Última actualización:** 4 de noviembre de 2025  
**Para más detalles:** Ver [GUIA_DESPLIEGUE.md](./GUIA_DESPLIEGUE.md)
